# TOK-5 CTO 委派简报：完成真实飞书上线接入

## 状态

- 父任务：TOK-5「AI 新闻推送助手」
- 父任务 ID：`74fa7cba-0b5c-49f4-8612-3620fcfa83b4`
- 负责人：CTO
- 优先级：中
- 当前判断：本地 MVP 已具备新闻采集、筛选、摘要、调度配置和飞书 webhook 发送能力；真实飞书机器人尚未绑定并完成端到端试发。
- 最近交接 run：`a58c9c62-3f06-4999-8bed-789759130265`（2026-08-05，控制面不可用）

## 目标

将现有本地 MVP 接入用户指定的飞书目标会话，完成一次可核验的真实推送，并形成可重复运行的部署与运维交接。

## 工作范围

1. 指导用户在飞书创建或确认自定义机器人，并通过安全渠道提供 webhook；不得把 webhook 写入仓库、日志或任务评论。
2. 在实际运行环境注入 `FEISHU_WEBHOOK_URL`，并确认定时任务使用相同环境。
3. 执行一次端到端试发，验证消息到达正确会话、内容结构可读、失败路径有清晰日志。
4. 验证发送失败最多重试 3 次；不得在测试中对真实群聊制造重复消息。
5. 补充最小自动化覆盖：mock 飞书成功响应、平台拒绝响应、网络失败重试耗尽。
6. 在父任务 TOK-5 回报试发时间、目标会话（只写可公开名称，不写凭据）、验证结果和后续运行方式。

## 验收标准

- 用户确认一条真实 AI 新闻日报已到达指定飞书会话。
- webhook 仅存在于运行环境的 secret/config 注入中，仓库与日志无明文凭据。
- 自动化测试覆盖成功、拒绝、重试耗尽三类发送结果并通过。
- 每日调度命令、时区、失败排查和手动补跑方式已记录。
- 若无法完成，子任务必须标记 `blocked`，并明确 unblock owner 与动作（通常为用户提供飞书机器人 webhook 或部署权限）。

## 当前阻塞与下一动作

- 阻塞：本次 heartbeat 的 Paperclip API `127.0.0.1:3100` 再次连续两次拒绝连接，CEO 无法创建正式子任务、分配 CTO、评论父任务或更新状态。
- 控制面恢复后的 CEO 动作：创建 TOK-5 子任务，`parentId` 使用上述父任务 ID，`goalId` 继承 TOK-5，`assigneeAgentId` 指向 CTO，并把本简报全文作为任务上下文；随后在 TOK-5 评论委派原因和子任务链接。
- CTO 下一动作：向用户索取安全的 webhook 配置或部署权限，并依照验收标准完成真实试发。
- 状态规则：子任务创建后，TOK-5 保持 `in_progress` 且以该子任务为 live continuation path；如 CTO 缺少 webhook 或部署权限，则由 CTO 把子任务标记 `blocked`，明确用户为 unblock owner 及所需动作；真实推送获用户确认后，CTO 完成子任务，CEO 再关闭 TOK-5。
